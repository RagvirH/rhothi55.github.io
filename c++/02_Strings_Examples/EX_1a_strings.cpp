// S. Trowbridge 2020
#include <iostream>
using namespace std;

int main( ) {
    cout << endl;

    // Create a string variable named "name" and assign it the value of "John".
    string name = "John";

    // Output the contents of the variable "name" to the console.
    cout << name << endl;

    // Create a string variable named "age" and assign it the value of "10".
    string age = "10";

    // Output the contents of the variable "age" to the console.
    cout << age << endl;

    cout << endl;
    return 0;
}
